fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
pydantic
passlib
# Pinned: passlib 1.7.4 reads bcrypt.__about__, which bcrypt removed in
# 4.1, so anything newer breaks password hashing at runtime.
bcrypt==4.0.1
python-jose
